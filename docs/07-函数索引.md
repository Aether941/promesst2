# 7. 函数索引

50 个函数按子系统分组。行号为定义行;标注“已裁”表示该实现被 `#if 0 / #elif 0` 关掉,当前编译不生效。

## 启动与平台层

| 行号 | 函数 | 职责 |
|---|---|---|
| L2523 | `void stbwingraph_main(void)` | 程序入口(经 STB_WINMAIN):建窗、隐藏光标、初始化图形、按 RELEASE 分支读档或直接开局,进入主循环。 |
| L2463 | `int winproc(void*, stbwingraph_event*)` | 窗口事件:按键→`queued_key`;destroy→自动存档;size/draw→重绘;失活/激活切换 `active`。 |
| L2432 | `int loopmode(float dt, int real, int in_client)` | 每帧回调:推进 `animcycle`,游戏内走 `timestep`,非游戏走 `process_metagame`,然后 `draw()`。 |
| L2420 | `void draw(void)` | `draw_init + draw_world + SwapBuffers` 提交一帧。 |
| L1572 | `void draw_init(void)` | 按窗口宽高比保持 1024×768 逻辑画面等比缩放,设置视口与投影。 |

## 初始化 / 关卡解析

| 行号 | 函数 | 职责 |
|---|---|---|
| L80 | `void init_graphics(void)` | 加载 `sprites/title/sss_logo.png` 为 OpenGL 纹理,并从精灵表 (y=96, x=112..119) 取样填充 `powers`。 |
| L299 | `void init_game(void)` | 把 `rawmap` 两层 ASCII 解析进 `tilemap/objmap`;处理出生点、物体、投影器字母→色/向;铺蛋瓦片;设 rover 初向。 |
| L1329 | `void restart_game(void)` | 清零 state(`gems_stored=-1`),重解析关卡并置出生房间已见。 |
| L466 | `void compute_room(void)` | 由 `px,py,pz` 求当前房间号。 |
| L475 | `void compute_powered(void)` | 重算 4×4 房间供电:有底座且全部被宝石占满 → 通电。 |

## 光照系统

| 行号 | 函数 | 职责 |
|---|---|---|
| L496 | `void propagate_light(void)` | 从每个有电投影器逐格播光,处理门阻挡/反射镜折射,卷绕世界,写 `light_for_dir/any_light_for_dir`。 |
| L538 | `int find_lights_on_player(point out[4])` | 找出所有照到玩家格的有电投影器(Z 键重定向目标列表)。 |
| L714 | `void get_abilities(uint8 enabled[8])` | 按玩家格四向入射光颜色统计当前可用能力。 |
| L91 | `void get_map_color(color*, int x,int y,int z)` | 按房间坐标从精灵表取样房间底色。 |

## 玩家移动与动作

| 行号 | 函数 | 职责 |
|---|---|---|
| L742 | `void move(int x, int y)` | 核心移动规则(远行/双步/开门/穿墙/碎石/拾魔杖/切层),见第 6 章 6.3。 |
| L855 | `void drop(void)` | X 键:拾取脚下宝石 / 把携带宝石放进底座,随后重算供电与光照。 |
| L876 | `void reflector(void)` | C 键:捡/放反射镜或切换斜向。 |
| L1026 | `void shoot(void)` | Z 键:把照到玩家的投影器朝向重定向到玩家朝向。另有 4 个被裁旧方案(L900/948/964/987)。 |
| L1056 | `void do_key(int ch)` | 把排队逻辑键分发给 move/drop/shoot/reflector/undo/Esc;含 DEBUG 作弊键。 |
| L684 | `void set_player_pos(int x, int y, int z)` | 换格统一入口:跨房间先 `save_state`,重算房间/供电/光照,标记 seen。 |
| L1079 | `int player_allowed(int x, int y)` | 判定玩家能否走向某格(供 Rover 冲突判定参考,当前调用已裁)。 |

## Rover 与旋转工具

| 行号 | 函数 | 职责 |
|---|---|---|
| L1093 | `int rove_allowed(int x, int y, int d, int rx, int ry)` | rover 能否走进某格:不出房间、非墙非门、目标为空或宝石。 |
| L1118 | `int preferred_rove_direction(uint8 occ[6][6], int x, int y, int rx, int ry)` | rover 首选方向:踩箭头则沿箭头;被堵/冲突则掉头,否则停留。 |
| L1140 | `void move_rovers(int rx, int ry)` | 单房间 rover 同步移动:声明意图→冲突迭代→落子;吃到宝石计分并触发喂食/反转计时。 |
| L726 | `int push_rovers(...)` | ⚠ 空壳,永远 return 0;调用处亦被 `#if 0`(推 rover 玩法未完成)。 |
| L1220 | `obj rotate_obj(obj o)` | 物体 dir 顺时针 +1(房间旋转动画用)。 |
| L1226 | `int rotate_tile(int t)` | 箭头瓦片顺时针旋转一格方向。 |

## 撤销 / 快照 / 存档

| 行号 | 函数 | 职责 |
|---|---|---|
| L613 | `void save_state(int rx, int ry, int rz)` | 进新房间前,把旧房间“十字物体带 + 门位图 + 玩家状态”压入 `undo_chain`。 |
| L641 | `void restore_undo(void)` | 退格:弹栈恢复十字带/门/玩家位置并重算房间、供电、光照。 |
| L675 | `void flush_undo(void)` | 清空整条撤销链。 |
| L587 | `void save_map(gamestate *g)` | 全图快照入 gamestate(checkpoint / 存档用)。 |
| L597 | `void restore_map(gamestate *g)` | 从快照恢复世界与玩家,重建房间/供电/光照。 |
| L1459 | `void load_game(void)` | 读 `save.dat`:restore_map + checkpoint + 重建撤销链;成功则 `game_started=1`。 |
| L1488 | `void save_game(void)` | 把 gamestate + checkpoint + 撤销链顺序落盘(RELEASE 才写)。 |
| L1410 / L1416 | `char *get_savegame_path(void)` | 存档路径:Win = `data2/save.dat`;Linux = XDG/HOME 下并逐级 mkdir(`#ifdef _WIN32` 双实现)。 |

## 计时 / 元游戏

| 行号 | 函数 | 职责 |
|---|---|---|
| L1237 | `void timestep(uint ms)` | 统一节拍器:递减 shot/feed/reverse/magic/rover/player/checkpoint 计时器,驱动房间旋转与 rover,攒 egg_timer,消费 queued_key。 |
| L1554 | `void process_metagame(float dt)` | 非游戏模式节拍:Logo 1500ms 倒计时自动切到主菜单。 |

## 菜单 / 选择

| 行号 | 函数 | 职责 |
|---|---|---|
| L1385 | `void get_choices(int enabled[8])` | 菜单项可用性:未开局禁 CONTINUE;没魔杖禁 “RESTORE FROM WAND GET”。 |
| L1396 | `void move_selection(int dir)` | 上下移动菜单光标,跳过不可选项。 |
| L1510 | `void do_metagame_key(int key)` | 菜单回车分发:继续 / 从 checkpoint 恢复 / 新游戏 / Credits / 存盘退出。 |

## 渲染(绘制)

| 行号 | 函数 | 职责 |
|---|---|---|
| L1851 | `void draw_world(void)` | 一帧主体:播光 → 画 8×8 窗口地形 → 物体/投影器 → 玩家(移动插值)→ 加法光束辉光 → 反射镜 → 蜥蜴与对话 → HUD → 结尾过场。 |
| L1725 | `void draw_metagame(float flicker)` | Logo(缩放+粒子背景)、主菜单(闪烁选中项)、滚动 Credits。 |
| L1620 | `void draw_rect(...)` | 在 GL_QUADS 中按精灵表 UV 画矩形。 |
| L1635 | `void draw_rectf(...)` | 浮点版矩形。 |
| L1651 | `void draw_sprite_raw(int x,int y,int s,int t)` | 按 16px 网格画整格精灵。 |
| L1659 | `void draw_sprite(...)` | 带颜色/透明度的精灵绘制。 |
| L1672 | `void draw_subsprite(...,int dx,int dy,...)` | 支持子像素偏移(移动/rover 平滑插值)。 |
| L1691 | `int draw_text(int x,int y,float size,char*,int spacing)` | 用内置字体逐字符画文本,返回结尾 x。 |
| L1707 | `int textlen(float size,char*,int spacing)` | 估算文本像素宽(居中排版用)。 |
| L1614 | `int blinn_8x8(int p1,int p2)` | 8.8 定点乘工具(渲染中未见调用)。 |
